<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <CheckBoxPreference
        android:key="color_blind"
        android:title="Color Blind Mode"
        android:summary="Change the route colors to a more color blind friendly color scheme"
        android:defaultValue="false" />

    <ListPreference
        android:key="stairs"
        android:title="Stairs"
        android:entries="@array/preferences"
        android:entryValues="@array/prefVal"
        android:summary="Choose to avoid, prefer, or ignore stairs"
        android:defaultValue="1" />

    <ListPreference
        android:key="outside"
        android:title="Outside"
        android:entries="@array/preferences"
        android:entryValues="@array/prefVal"
        android:summary="Choose to avoid, prefer, or ignore outside paths"
        android:defaultValue="1" />

    <ListPreference
        android:key="speed"
        android:title="Walking Speed"
        android:entries="@array/walkSpeed"
        android:entryValues="@array/walkValues"
        android:summary="How fast you walk, used for calculating time to destination"
        android:defaultValue="4.5" />
</PreferenceScreen>